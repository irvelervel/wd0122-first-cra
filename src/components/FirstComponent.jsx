const FirstComponent = () => {
  return (
    <div>
      <h2>New Component here!</h2>
    </div>
  )
}

export default FirstComponent

// to create a valid React Component, write the function (remember! use the same
// name as the file, starting with a capital letter) and EXPORT IT at the end
// of the file: in this way, your component can be used in another one
